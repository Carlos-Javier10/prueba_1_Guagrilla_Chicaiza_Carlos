//traemos el paquete
const { Router } = require('express');
const data = require("../Data.json")
//iniciamos el route
const router = Router()

// crecion de los endpoints
router
    .get("/", (req, res)=>{
        
    })